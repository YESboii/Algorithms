package stack;
//https://leetcode.com/problems/valid-parentheses/description/
public class ValidParentheses {
    //for each opening push a closing bracket
    //if we encounter a closing bracket, the check if the stack is empty, it means, there was no opening one,
    //or if the top of the stack has some other bracket mean closed in wrong order
    //at last, return stack.isEmpty() top handle cases where there are no/more opening than closing brackets.
}
